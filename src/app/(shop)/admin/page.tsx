/** Los administradores podran manejar los productos y demas. Recordemos que estoy trabaando shop como una carpeta simplemente, no es una ruta,
 * por lo tanto esta pagina corresponde a http://localhost:3000/admin
 */

export default function AdminPage() {
  return (
    <div>
      <h1>Admin page</h1>
    </div>
  );
}