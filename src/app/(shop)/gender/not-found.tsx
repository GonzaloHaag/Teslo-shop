/** pagina de error en caso de que el genero no existe, este not Found es solo 
 * para los generos
 */

import { PageNotFound } from "@/components";

export default function NotFoundGenderPage() {
  return (
    <PageNotFound />
  );
}