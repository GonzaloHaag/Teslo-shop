/**  Recordemos que estoy trabaando shop como una carpeta simplemente, no es una ruta,
 * por lo tanto esta pagina corresponde a http://localhost:3000/products
 */

export default function ProductsPage() {
  return (
    <div>
      <h1>Products Page</h1>
    </div>
  );
}